# ══════════════════════════════════════════════════════════════════
# EISForge — Requirements
# Author: Hoda Jafari | May 2026
# ══════════════════════════════════════════════════════════════════

# ── Core Scientific Stack ─────────────────────────────────────────
numpy>=1.26.0
scipy>=1.11.0
pandas>=2.1.0
pyarrow>=14.0.0          # ذخیره دیتاست به فرمت parquet

# ── EIS-Specific ──────────────────────────────────────────────────
impedance>=1.4.1          # CNLS fitting + K-K validation
                          # NOTE: 1.7.1 is the newest PyPI release (2023-07-10).
                          # Its eval_linKK is broken under numpy>=2.0
                          # (NameError: np not defined in eval; upstream issue
                          # #318, fixed on master by PR #322 but unreleased as
                          # of 2026-08-05), so the K-K validator relies on the
                          # Voigt-circuit fallback in practice.

# ── Machine Learning ──────────────────────────────────────────────
torch>=2.1.0              # PyTorch — EIS-GPT Transformer
scikit-learn>=1.3.0       # Random Forest + preprocessing
xgboost>=2.0.0            # Gradient Boosting

# ── Visualization ─────────────────────────────────────────────────
plotly>=5.18.0            # Nyquist و Bode plots تعاملی
matplotlib>=3.8.0         # رسم static
kaleido>=0.2.1            # export تصویر از plotly

# ── Web UI ────────────────────────────────────────────────────────
streamlit>=1.30.0         # رابط کاربری وب

# ── Instrument Parsers ────────────────────────────────────────────
galvani>=0.4.0            # BioLogic .mpr/.mpt

# ── Parallel Processing ───────────────────────────────────────────
joblib>=1.3.0             # batch processing موازی

# ── Export ────────────────────────────────────────────────────────
openpyxl>=3.1.0           # Excel export
reportlab>=4.0.0          # PDF report

# ── Development ───────────────────────────────────────────────────
pytest>=7.4.0
black>=23.0.0
